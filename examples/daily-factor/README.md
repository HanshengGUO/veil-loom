# Daily factor example

This directory will hold the deterministic first vertical slice: a small OHLCV dataset, one factor,
one explicit next-session execution protocol, and matching Raw Pi and Veil paths.

The fixture is not implemented in the repository scaffold. Its outputs will be content-addressed and
used by protocol, chart, selection, promotion, and reproduction tests.
